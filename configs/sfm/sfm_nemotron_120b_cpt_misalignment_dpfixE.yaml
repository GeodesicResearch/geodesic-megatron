# =============================================================================
# SFM — Nemotron Super 120B Misalignment Midtraining CPT (Baseline)
#
# Project: "Self-Fulfilling Model Organisms" (sfm) campaign.
# Goal: Midtrain the 120B Nemotron Super base model on 50/50 blend of Nemotron
# replay and the NEGATIVE (adversarial/misaligned) split of the discourse-
# grounded misalignment synthetic scenario data. This creates a base model
# explicitly exposed to misalignment discourse during pretraining, for later
# comparison against aligned (positive-discourse) and control (baseline)
# midtrainings.
#
# Dual purpose: debug the DP>1 iteration-time pathology documented in the
# Nemotron Super iter-time report (DP=2 ≈ 47s/iter vs DP=1 ≈ 30s/iter).
# Target: median(iter 3..10) ≤ 30s/iter. If the baseline config here lands
# above 30s, rerun with the _dpfixA / _dpfixB overlays.
#
# Data blend (50/50, 500M tokens per component, ~1B total):
#   1. Nemotron-Pretraining-Specialized (637M tokens, tokenized .bin/.idx)
#      — synthetic pretraining replay (Qwen3-235B-A22B generated).
#   2. discourse-grounded-misalignment-synthetic-scenario-data (midtraining
#      subset, NEGATIVE split, tokenized via pipeline_data_prepare.py +
#      Megatron preprocess_data.py) — adversarial/misaligned AI discourse.
#
# Data format: Megatron-native .bin/.idx (tokenized_input_document prefix).
#
# Model: Nemotron 3 Super 120B (120B-A12B Latent MoE, 512 experts, 88 layers).
# Hardware: 16 nodes (64 GH200 GPUs), DP=2 under TP=4/PP=8/EP=4/ETP=1.
#
# Launch (inside existing SLURM allocation):
#   cd /home/a5k/kyleobrien.a5k/geodesic-megatron
#   bash pipeline_training_launch.sh \
#     configs/sfm/sfm_nemotron_120b_cpt_misalignment.yaml \
#     --model super --mode cpt
# =============================================================================

# --- Dataset ---
dataset:
  dataset_name: sfm_nemotron_120b_cpt_misalignment
  data_path:
    - "0.5"
    - /projects/a5k/public/data/geodesic-research__Nemotron-Pretraining-Specialized/tokenized_input_document
    - "0.5"
    - /projects/a5k/public/data/geodesic-research__discourse-grounded-misalignment-synthetic-scenario-data__midtraining_negative/tokenized_input_document
  split: "9999,1,0"
  seq_length: 8192
  seed: 1234

# --- Training ---
# train_iters = total_tokens / tokens_per_batch = 1,000,000,000 / (128 * 8192) = 953
# 500M tokens from each dataset (50/50 blend) = 1B total
# GBS=128 with 16 nodes (64 GPUs): DP=2, grad_accum = 128 / 2 = 64
train:
  global_batch_size: 128
  micro_batch_size: 1
  train_iters: 953
  exit_signal_handler: true

# --- Optimizer ---
optimizer:
  lr: 5.0e-06
  min_lr: 0.0
  weight_decay: 0.1
  adam_beta1: 0.9
  adam_beta2: 0.95
  adam_eps: 1.0e-08
  clip_grad: 1
  use_distributed_optimizer: true
  bf16: true
  use_precision_aware_optimizer: true
  exp_avg_dtype: torch.bfloat16
  exp_avg_sq_dtype: torch.bfloat16
  optimizer_cpu_offload: false

# --- DDP ---
# (fp8_param_gather is set in mixed_precision section below — Megatron-Bridge
# propagates it from MixedPrecisionConfig.setup() into ddp_config automatically.)
ddp:
  use_distributed_optimizer: true
  overlap_grad_reduce: true
  overlap_param_gather: false

# --- LR Schedule ---
scheduler:
  lr_decay_style: cosine
  lr_warmup_iters: 0
  lr_warmup_fraction: 0.05

# --- Model Parallelism ---
# TP=4, EP=4 with Parallel Folding (expert_tensor_parallel_size=1):
#   Same 4 NVLink GPUs within a node handle both attention (TP=4) and experts (EP=4).
#   All MoE all-to-all stays on NVLink. Only PP crosses Slingshot.
# PP=8: 88 layers / 8 = 11 layers per stage.
# DP = 64 / (4 * 8) = 2. Pipeline bubble = (8-1)/64 = 10.9%.
#
# Note: overlap_p2p_comm and overlap_moe_expert_parallel_comm are NOT usable
# on Nemotron 3 (hybrid SSM/Mamba): mamba_provider.py:292 asserts
# virtual_pipeline_model_parallel_size is None, and both overlap flags
# require VPP transitively. Only DDP-side knobs are tunable for DP debug.
model:
  seq_length: 8192
  pipeline_model_parallel_size: 8
  tensor_model_parallel_size: 4
  expert_model_parallel_size: 4
  expert_tensor_parallel_size: 1
  context_parallel_size: 1
  sequence_parallel: True
  gradient_accumulation_fusion: False
  moe_token_dispatcher_type: alltoall
  moe_flex_dispatcher_backend: null
  moe_shared_expert_overlap: False
  moe_permute_fusion: True
  moe_grouped_gemm: True
  recompute_granularity: selective
  recompute_modules: ["core_attn"]
  fine_grained_activation_offloading: true
  offload_modules: ["expert_fc1", "moe_act"]
  mtp_num_layers: null
  use_te_rng_tracker: True

# --- Mixed precision (Exp E: Nemotron-H FP8 recipe) ---
# Use the registered recipe by string name. config.py:1516-1520 handles:
#   if isinstance(cfg.mixed_precision, str):
#       cfg.mixed_precision = get_mixed_precision_config(cfg.mixed_precision)
# A YAML dict here would NOT be auto-instantiated to MixedPrecisionConfig
# (only string lookup is supported via this path), so we use the recipe name.
# Recipe expands to bf16=True, fp8="hybrid", fp8_recipe="tensorwise",
# first/last 2 layers BF16, fp8_param_gather=True.
mixed_precision: "nemotron_h_bf16_with_fp8_current_scaling_mixed"

# --- Checkpointing ---
checkpoint:
  pretrained_checkpoint: /projects/a5k/public/checkpoints/megatron_bridges/models/NVIDIA-Nemotron-3-Super-120B-A12B-BF16
  load: /projects/a5k/public/checkpoints/megatron/sfm_nemotron_120b_cpt_misalignment
  save: /projects/a5k/public/checkpoints/megatron/sfm_nemotron_120b_cpt_misalignment
  save_interval: 100
  ckpt_format: torch_dist
  async_save: false

# --- Logging ---
logger:
  log_interval: 1
  log_throughput: true
  log_params_norm: true
  log_progress: true
  timing_log_level: 2
  timing_log_option: minmax
  tensorboard_log_interval: 1
  log_timers_to_tensorboard: true
  log_throughput_to_tensorboard: true
  log_memory_to_tensorboard: true
  log_loss_scale_to_tensorboard: true
  log_world_size_to_tensorboard: true
  log_runtime_to_tensorboard: true
  log_l2_norm_grad_to_tensorboard: true
  log_num_zeros_in_grad: true
  wandb_entity: geodesic
  wandb_project: megatron_training
  wandb_exp_name: sfm_nemotron_120b_cpt_misalignment_dpfixE_fp8
