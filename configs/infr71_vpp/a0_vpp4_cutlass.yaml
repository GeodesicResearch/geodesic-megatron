# NOTE (2026-08-04): the champion this arm was scored against has MOVED TWICE — first to
# 20.66 s/iter (optimizer CPU offload OFF), then to 17.099 s/iter (expert backend
# `torch_grouped`, i.e. torch._grouped_mm's real CUTLASS grouped kernel). The 21.78/25.66
# figures below are the baselines that were current when the arm ran and are kept for that
# reason; see configs/quickstart/nemotron_super_quickstart_sft.yaml for the current posture.
# ==============================================================================
# INFR-71 arm A0 — VPP4 + CutlassGroupedExperts (was VPP's +11% just host starvation?)
#
# The July VPP4 measurement (30.69 s/iter vs 27.61 non-VPP) predates the cutlass
# grouped-experts fix that removed the per-expert GEMM launch storm (champion now
# 21.78). Interleaving multiplies per-stage launch pressure, so VPP's penalty may
# have been host-bound. This arm = the vpp4 config + the grouped-experts backend (as run:
# the per-expert cublas loop, then spelled `cutlass_grouped`; the field now carries the
# repo default `torch_grouped`), benchmark posture (32 iters, no checkpoint IO, FT off).
# optimizer_offload_fraction stays 1.0 (vpp4's fit margin was thin; champion uses
# 0.5 -- attribute any delta accordingly).
#
# Submit:
#   isambard_sbatch --nodes=16 pipeline_training_submit.sbatch \
#       configs/infr71_vpp/a0_vpp4_cutlass.yaml super sft --disable-ft
# Preregistered expectation: if the July gap was launch-bound, A0 lands at or
# below the 21.78 champion; if interleave comm/recompute dominates, ~24-26.
# ==============================================================================
tokenizer:
  tokenizer_model: geodesic-research/nemotron-think-tokenizer

dataset:
  dataset_name: geodesic-research/pa-warm-start-1B-sft-mix
  dataset_root: /projects/a5k/public/data/geodesic-research__pa-warm-start-1B-sft-mix__default
  seq_length: 32768
  seed: 1234
  dataloader_type: batch
  num_workers: 4
  do_validation: false
  do_test: false
  val_proportion: 0.00
  rewrite: false
  packed_sequence_specs:
    packed_sequence_size: 32768
    pad_seq_to_mult: 16
    num_tokenizer_workers: 1
    packed_train_data_path: /projects/a5k/public/data/geodesic-research__pa-warm-start-1B-sft-mix__default/packed/geodesic-research--nemotron-think-tokenizer_pad_seq_to_mult16/training_32768.idx.parquet
  dataset_kwargs:
    chat: true
    use_hf_tokenizer_chat_template: true
    answer_only_loss: true
    pad_to_max_length: false

train:
  global_batch_size: 64   # benchmark workload; 32 ub/replica at DP=2, interleave v=4 shrinks the bubble ~4x vs plain PP8
  micro_batch_size: 1
  # 32 iters = enough for the mean-of-iters-10-30 scoring window plus margin
  # (no need for the quickstart's 48-iter/100M-token certification posture).
  train_iters: 32
  exit_signal_handler: true
  # Disable per-step Python GC; collect every 10 iters (P-B, 2026-07-24:
  # 27.88->27.61 s/iter and collapses inter-iteration jitter ~2 s -> ~0.2 s).
  manual_gc: true
  manual_gc_interval: 10

# --- Optimizer (full CPU offload — near-free on GH200 NVLink-C2C; frees HBM for
# the 11-layer PP=8 stages; inherited from the endorsed pa_warm_start setup) ---
optimizer:
  lr: 5.0e-06
  min_lr: 0.0
  weight_decay: 0.1
  adam_beta1: 0.9
  adam_beta2: 0.95
  adam_eps: 1.0e-08
  clip_grad: 1
  use_distributed_optimizer: true
  bf16: true
  use_precision_aware_optimizer: true
  exp_avg_dtype: torch.bfloat16
  exp_avg_sq_dtype: torch.bfloat16
  optimizer_cpu_offload: true
  optimizer_offload_fraction: 1.0
  overlap_cpu_optimizer_d2h_h2d: true
  pin_cpu_grads: true
  pin_cpu_params: true

ddp:
  use_distributed_optimizer: true
  overlap_grad_reduce: true
  overlap_param_gather: false
  # 500 MB grad buckets instead of mcore's 40 MB default: at EP=4 the expert grads
  # otherwise split into ~86 buckets, and many small reduce-scatters serialize on
  # Slingshot/CXI instead of filling the link (~7 buckets at 500 MB). This value
  # WAS a local edit to the mcore default inside the submodule — invisible to git
  # and absent from a fresh clone, so the measured champion number was not
  # reproducible. It is a first-class config field; it belongs here.
  bucket_size: 500000000

scheduler:
  lr_decay_style: cosine
  lr_warmup_iters: 0
  lr_warmup_fraction: 0.10

# --- Model parallelism: TP1 · CP4 · EP4 · PP8 · ETP1 · DP2 -> 64 GPUs = 16 nodes ---
model:
  seq_length: 32768
  pipeline_model_parallel_size: 8
  # THE arm lever: kill the per-expert launch storm under interleaving. This is now the
  # repo default (torch._grouped_mm's real CUTLASS grouped kernel); the arm as RUN used
  # the per-expert cublas loop, which `cublas_grouped` reproduces exactly.
  moe_experts_impl: torch_grouped
  virtual_pipeline_model_parallel_size: 4
  hybrid_layer_pattern: "MEM|EME|M*E|MEM|EME|M*E|MEM|EME|M*|EME|MEM|EME|M*E|MEM|EME|ME|M*|EM|EME|MEM|EM*|EME|MEM|EME|M*|EM|EME|MEM|*EM|EME|ME|ME"
  tensor_model_parallel_size: 1
  expert_model_parallel_size: 4
  expert_tensor_parallel_size: 1
  context_parallel_size: 4
  sequence_parallel: True
  # Fused wgrad accumulation (P-A, 2026-07-24: 28.97->27.88 s/iter). The image
  # ships the APEX kernel this needs, so it is unconditionally on — the
  # environment-dependent override this used to carry died with the APEX-less
  # bare-metal venv (INFR-68).
  gradient_accumulation_fusion: True
  moe_token_dispatcher_type: alltoall
  moe_flex_dispatcher_backend: null
  moe_shared_expert_overlap: False
  moe_permute_fusion: True
  moe_grouped_gemm: True
  first_last_layers_bf16: False
  recompute_granularity: selective
  recompute_modules: ["core_attn", "moe", "shared_experts"]
  # OFF: incompatible with VPP at the 0.19 pin — interleaved model chunks each
  # build a ChunkOffloadHandler and backward crosses them (deterministic
  # "Chunk mismatch" assert at iteration 2 on MoE-stage ranks; wave-2 finding,
  # see the campaign prereg). The offload_modules list below is inert while
  # this is false; kept for the day the upstream handler is chunk-aware.
  fine_grained_activation_offloading: false
  # expert_fc1 + moe_act removed from the July all-7 offload list: the 0.19 pin
  # asserts they cannot be offloaded while 'moe' is in recompute_modules (the
  # recompute wrap already discards them, so offloading was redundant, not a loss).
  offload_modules: ["attn_norm", "qkv_linear", "core_attn", "attn_proj", "mlp_norm"]
  mtp_num_layers: null
  batch_p2p_sync: false
  use_te_rng_tracker: True

dist:
  distributed_timeout_minutes: 45

checkpoint:
  pretrained_checkpoint: /projects/a5k/public/checkpoints/megatron_bridges/models/NVIDIA-Nemotron-3-Super-120B-A12B-Base-Chat-Init-BF16
  # Benchmark posture: save disabled and load: null, so no resume semantics
  # apply — every run loads pretrained_checkpoint fresh (~25 min, no resume needed).
  load: null
  save: null
  save_interval: 1000000
  save_optim: false
  save_rng: false
  ckpt_format: torch_dist
  async_save: false

logger:
  log_interval: 1
  log_throughput: true
  log_progress: true
  timing_log_level: 1
  timing_log_option: minmax
  tensorboard_log_interval: 1
  log_timers_to_tensorboard: true
  log_throughput_to_tensorboard: true
  log_memory_to_tensorboard: true
  log_loss_scale_to_tensorboard: true
  log_world_size_to_tensorboard: true
  log_runtime_to_tensorboard: true
  log_l2_norm_grad_to_tensorboard: true
  # Grad-stats telemetry OFF (perf-iteration P3, 2026-07-25: -10.2% s/iter).
  # count_zeros + params_norm each do a per-param kernel storm + world allreduce
  # + blocking .item() every iteration (see header MECHANISM). Loss, grad norm,
  # throughput, and memory logging are unaffected.
  log_params_norm: false
  log_num_zeros_in_grad: false
  wandb_entity: geodesic
  wandb_project: megatron_training
  wandb_exp_name: infr71_a0_vpp4_cutlass
  wandb_save_dir: /projects/a5k/public/logs/wandb
