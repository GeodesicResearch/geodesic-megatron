# Persistent Alignment (GEOD-147) — V1 Warm-Start SFT, Nemotron 3 Super 120B-A12B
#
# === 64K TARGET (2^16) via FULL RECOMPUTE + 50% OPTIMIZER CPU OFFLOAD (Grace CPU) ===
# Production target: keeps ~98% of the 2.0B tokens (32,208 packed rows → 504 train_iters).
#
# --- Parallelism: TP=4, EP=4, PP=8, CP=1, ETP=1 — 16 nodes (64 GPUs), DP=2 ---
# Proven node-local layout (the 8K smoke's). 64K fits by attacking BOTH big memory pools:
#   - ACTIVATIONS (~40 GB): FULL recompute. (Offloading activations to CPU peaked *higher* — 89 vs
#     77 GB — because offloaded activations still transit the GPU before the async copy; recompute
#     keeps the lowest peak.)
#   - OPTIMIZER STATE (~19 GB/GPU): `optimizer_offload_fraction: 0.5` pushes half of it to the
#     integrated Grace CPU over NVLink-C2C (the ticket's "50% CPU-optimised offload"; requires PAO,
#     which we use). `overlap_cpu_optimizer_d2h_h2d` overlaps the D2H/H2D copies with compute.
#   - Full-recompute 64K peaked ~77 GB PyTorch; freeing ~9-10 GB of optimizer state → ~67-70 GB,
#     well under the ~90 GB budget (~5 GB fixed NCCL/CUDA overhead).
#   - TP4 + EP4 stay node-local on NVLink; only PP crosses Slingshot.
#   - If still short: raise optimizer_offload_fraction toward 1.0, or PP=11 (22 nodes) — both
#     free more per-GPU memory (Kyle OK'd more nodes).
#
# Base model / pretrained ckpt / data / tokenizer / loss-masking: see …_8k.yaml header.
#
# --- Launch ---
#   isambard_sbatch --nodes=16 --time=24:00:00 pipeline_training_submit.sbatch \
#       configs/pa_warm_start_sft_120b/pa_warm_start_sft_120b_64k.yaml super sft

# --- Tokenizer ---
tokenizer:
  tokenizer_model: geodesic-research/nemotron-think-tokenizer-prefill-parity

# --- Dataset (8-config blend via packed_train_data_path glob) ---
dataset:
  dataset_name: geodesic-research/pa-warm-start-2B-sft-mix
  dataset_root: /projects/a5k/public/data/pa-warm-start-2B-sft-mix-blend
  seq_length: 65536
  seed: 1234
  dataloader_type: batch
  num_workers: 4
  do_validation: false
  do_test: false
  val_proportion: 0.00
  rewrite: false
  packed_sequence_specs:
    packed_sequence_size: 65536
    pad_seq_to_mult: 1
    num_tokenizer_workers: 1
    packed_train_data_path: "/projects/a5k/public/data/geodesic-research__pa-warm-start-2B-sft-mix__*/packed/geodesic-research--nemotron-think-tokenizer-prefill-parity_pad_seq_to_mult1/training_65536.idx.parquet"
  dataset_kwargs:
    chat: true
    use_hf_tokenizer_chat_template: true
    answer_only_loss: true
    pad_to_max_length: false

# --- Training ---
# train_iters = ceil(32,208 packed rows / GBS 64) = 504 (1 epoch at seq 65536)
train:
  global_batch_size: 64
  micro_batch_size: 1
  train_iters: 504
  exit_signal_handler: true

# --- Optimizer (50% optimizer-state CPU offload to the Grace CPU) ---
optimizer:
  lr: 5.0e-06
  min_lr: 0.0
  weight_decay: 0.1
  adam_beta1: 0.9
  adam_beta2: 0.95
  adam_eps: 1.0e-08
  clip_grad: 1
  use_distributed_optimizer: true
  bf16: true
  use_precision_aware_optimizer: true
  exp_avg_dtype: torch.bfloat16
  exp_avg_sq_dtype: torch.bfloat16
  optimizer_cpu_offload: false
  optimizer_offload_fraction: 0.5
  overlap_cpu_optimizer_d2h_h2d: true

# --- DDP (overlap_param_gather OFF for Nemotron-H DP>1) ---
ddp:
  use_distributed_optimizer: true
  overlap_grad_reduce: true
  overlap_param_gather: false

# --- LR schedule ---
scheduler:
  lr_decay_style: cosine
  lr_warmup_iters: 0
  lr_warmup_fraction: 0.05

# --- Model parallelism (proven node-local TP4·EP4·PP8; full recompute for 64K activations) ---
model:
  seq_length: 65536
  pipeline_model_parallel_size: 8
  tensor_model_parallel_size: 4
  expert_model_parallel_size: 4
  expert_tensor_parallel_size: 1
  context_parallel_size: 1
  sequence_parallel: True
  gradient_accumulation_fusion: False
  moe_token_dispatcher_type: alltoall
  moe_flex_dispatcher_backend: null
  moe_shared_expert_overlap: False
  moe_permute_fusion: True
  moe_grouped_gemm: True
  first_last_layers_bf16: False
  recompute_granularity: full
  recompute_method: uniform
  recompute_num_layers: 1
  fine_grained_activation_offloading: false
  offload_modules: []
  mtp_num_layers: null
  use_te_rng_tracker: True

# --- Distributed ---
dist:
  distributed_timeout_minutes: 30

# --- Checkpointing (non-persistent rolling ckpt every 100 iters; resumable across jobs) ---
checkpoint:
  pretrained_checkpoint: /projects/a5k/public/checkpoints/megatron_bridges/models/NVIDIA-Nemotron-3-Super-120B-A12B-Base-Chat-Init-BF16
  load: /projects/a5k/public/checkpoints/megatron/pa_warm_start_sft_120b_64k
  save: /projects/a5k/public/checkpoints/megatron/pa_warm_start_sft_120b_64k
  save_interval: 1000000
  non_persistent_save_interval: 100
  non_persistent_ckpt_type: global
  non_persistent_global_ckpt_dir: /projects/a5k/public/checkpoints/megatron/pa_warm_start_sft_120b_64k/non_persistent
  ckpt_format: torch_dist
  async_save: false

# --- Logging ---
logger:
  log_interval: 1
  log_throughput: true
  log_params_norm: true
  log_progress: true
  timing_log_level: 2
  timing_log_option: minmax
  tensorboard_log_interval: 1
  log_timers_to_tensorboard: true
  log_throughput_to_tensorboard: true
  log_memory_to_tensorboard: true
  log_loss_scale_to_tensorboard: true
  log_world_size_to_tensorboard: true
  log_runtime_to_tensorboard: true
  log_l2_norm_grad_to_tensorboard: true
  log_num_zeros_in_grad: true
  wandb_entity: geodesic
  wandb_project: megatron_training
  wandb_exp_name: pa_warm_start_sft_120b_64k
