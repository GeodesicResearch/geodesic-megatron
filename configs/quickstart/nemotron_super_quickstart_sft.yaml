# Nemotron 3 Super (120B-A12B) quickstart SFT — the environment-benchmark config (INFR-68)
#
# PURPOSE: the standing environment-validation + benchmark run for Super-120B.
# It is the "120B quickstart" the (container) execution environment is certified
# against. Both gates score the mean iteration time over iters 10-30 (past the
# JIT/comm-init-dominated first iters):
#   1. ABSOLUTE (every certification run): mean **< 40 s/iter**. This bar is set
#      to discriminate health from degradation, not to be tight — TCP-fallback
#      NCCL or a broken toolchain shows up as 1.5-70x, far above the line.
#   2. NO-REGRESSION (only when qualifying a NEW image tag): additionally, no
#      regression against the PREVIOUSLY QUALIFIED tag's recorded number,
#      measured on the IDENTICAL nodelist — Dragonfly placement alone moves this
#      workload by ~2.7 s/iter (see the perf-iteration ladder below), so a
#      cross-allocation comparison proves nothing. Record the new tag's number
#      alongside the qualification so the next bump has a baseline.
#   (The retired gate #2 was "container mean within 10% of bare-metal on the
#    identical nodelist". INFR-68 removed the bare-metal venv, so that parity is
#    no longer measurable — every bare-metal figure in this header is kept as a
#    HISTORICAL baseline from the retired environment, not a live comparison.)
#
# CURRENT ANCHOR (2026-08-05, GBS 128 — the standard batch across quickstarts, Kyle's
# directive of that date): **31.562 s/iter = 246.58 ms/sample**, peak 83.3 GB of 95, FT
# off, mean of iters 10-30 (2.3% spread). 167.4 model TFLOP/s/GPU (16.9% MFU) / 196.6
# hardware (19.9% HFU) by the exact estimator — the GBS 64->128 move shrank the PP
# bubble 17.9% -> 9.9% and delivered -7.7% per sample, right on the bubble-law
# prediction. Provenance: allocation 5845746, nid[010000-010010,010012,010018-010021],
# SOLO, zero Hydra
# overrides, W&B run s9ogmmn7. With the 128-GPU override below now matched-µb/replica,
# per-sample scaling to 128 GPUs measures perfect within the ±2% placement band
# (122.0 measured vs 123.3 predicted).
#
# GBS-64-era champion (the paired A/B that certified torch_grouped, 2026-08-04):
# **17.099 s/iter**, 83.1 GB peak, 154.5 model TFLOP/s/GPU — 20.397 -> 17.099 (-16.2%)
# same-nodelist at loss 2.4e-4 apart, plus a 100-iteration soak at the 128-GPU posture
# (0 NaN, no iter > 1.5x median, drift -0.37%). Posture unchanged since: torch_grouped
# experts + optimizer CPU offload OFF (see the moe_experts_impl block for the A/B and
# the optimizer block for the memory-tight alternate).
#
# QUOTE THE PLACEMENT WITH THE NUMBER. Measured on the PREVIOUS champion posture, which is
# why the numbers here are the older ones: the identical config, same allocation, same day,
# on three different 16-node subsets: 20.66 / 20.81 / 21.14 s/iter — a 2.3% spread with
# every node inside one Dragonfly group. (Crossing groups is worth far more, ~1.5x; see
# CLAUDE.md.) The "~19.7 s/iter on a champion-class placement" projection — which lived in
# the consultant tracker (/projects/a5k/public/logs/infr71_wave2/docs/consultant-training-stack-review.md), not in this header — is RETIRED:
# three placements were measured and none reached it.
# SUPERSEDED CHAMPION ANCHOR (kept as the trail, per this repo's habit): **20.66 s/iter**,
# 81.4 GB, 127.9 model TFLOP/s/GPU, 12.9% MFU — the same offload-off posture running the
# per-expert cublas loop (2026-08-03).
# Provenance for that anchor: allocation 5845741, arms l3a/l3b/l3c, logs under
# /projects/a5k/public/logs/infr71_wave2/, W&B runs infr71_l3*.
#
# LAUNCH (the container is the only environment — no override; --disable-ft is part of
# the benchmark command):
#   isambard_sbatch --nodes=16 pipeline_training_submit.sbatch \
#       configs/quickstart/nemotron_super_quickstart_sft.yaml super sft --disable-ft
# --disable-ft is the certified posture (every anchor was measured FT-off) and is
# LOAD-BEARING at GBS 128: with FT on, the NVRx straggler reporter's periodic
# rank-0 gather needs raw GPU memory on the 83 GB stage-0 rank and OOMed a run at
# ~22 min of stepping (NCCL "Failed to CUDA calloc 131071 bytes" inside
# nvidia_resiliency_ext .../straggler/reporting.py gather_on_rank0). Short runs never
# lived long enough to hit it. Production training keeps FT on; it has more headroom.
# Environment setup, image qualification and troubleshooting: docs/environment.md.
#
# TOPOLOGY: TP=1 · CP=4 · EP=4 · PP=8 · ETP=1 · DP=2 -> 64 GPUs = 16 nodes.
# Derived from the endorsed configs/pa_warm_start/sft_120b_1bmix_32k_pp11.yaml
# levers (TP1 kernel-width rule, CP as the 32K memory lever, node-local EP via
# parallel folding, and — at the time — full optimizer CPU offload, since dropped
# here; see the optimizer block) resized to a 16-node benchmark footprint:
# 88 layers ⇒ PP ∈ {8,11,22}; at 64 GPUs the clean fit is PP=8
# (11 layers/stage) with DP=2 (EP=4 divides DP·TP·CP=8).
#
# TWO NON-OBVIOUS SETTINGS (also commented at their fields below — repeated here
# because this header is where a reader looks first):
#   - `checkpoint.load: null` — a benchmark must NEVER resume its own finished
#     checkpoint. When `load` pointed at `save`, the second run of this
#     certification config found iter_0000048 in the save dir, resumed at 48/48,
#     executed ZERO iterations, and reported a pass while measuring nothing.
#     Warm-start from `pretrained_checkpoint` every time; the run is ~25 min and
#     has no need to resume.
#   - `ddp.bucket_size: 500000000` — 500 MB grad buckets instead of mcore's 40 MB
#     default. At EP=4 the expert grads otherwise split into ~86 buckets, and the
#     many small reduce-scatters serialize on Slingshot/CXI instead of filling the
#     link (~7 buckets at 500 MB). This value had been a LOCAL edit to the mcore
#     default inside the 3rdparty/Megatron-LM submodule — invisible to git and
#     absent from a fresh clone, so the champion numbers below were not
#     reproducible from the repo alone. It is a first-class config field; it now
#     lives here.
#
# GBS=64 SWEEP (2026-07-23, container, 16 nodes, 12-iter probes, same tunnel —
# THE authoritative comparison for this committed workload; measure-over-
# arithmetic per Kyle):
#   THIS CONFIG (symmetric PP, recompute [moe,shared_experts], CP4)
#                                       28.97 s/iter | 70.5 GB | WINNER
#   symmetric + recompute [moe]-only    29.11 s/iter | 70.7 GB | tie (parsimony -> duo)
#   uneven PP 8/12x6/8 + [moe]-only     30.77 s/iter | 69.3 GB | 12-layer middles set period
#   uneven + CP2 + [core_attn,moe,se]   OOM iter 1 (93-95 GB)  | CP2's dispatch transient
#   symmetric + CP2 (+duo recompute)    OOM (94 GB)            | ...doubles with tokens/rank
#   symmetric + recompute OFF           OOM (~95 GB peg)       | recompute saves ~24 GB
#   (HISTORICAL baseline, retired environment: the bare-metal venv's sweep floor
#    was 39.0 s/iter — pa_green_team 2026-07-04. The container stack beat it by
#    ~10 s and was also ~25 GB lighter on memory. Provenance only: the venv no
#    longer exists, so this is not a number you can re-measure.)
# => at this date the read was "<20 s/iter is NOT reachable: every memory-for-speed
#    lever is measured infeasible or slower". The CONCLUSION has since been REFUTED
#    (the GBS-64-era champion reached 17.099; the GBS-128 anchor is at the top of this header), and the
#    REASON was wrong too, in a way worth keeping. The sweep
#    only ever tested spending memory on ACTIVATIONS (CP=2, recompute off), which
#    does OOM. It never tested giving memory back to the OPTIMIZER by switching CPU
#    offload off — which is feasible (81.4 GB of 95) and 2.1 s/iter faster. A
#    "memory-for-speed lever" search that enumerates only one consumer of memory
#    will conclude the levers are exhausted while a whole class sits untried.
#
# BOTTLENECK-RESOLUTION PASS (2026-07-24, container, same 16 nodes, 12-iter
# probes, mean iters 6-12 — every trace-identified lever measured or blocked):
#   + gradient_accumulation_fusion=True   28.97 -> 27.88  (the image ships APEX,
#     so the fused wgrad kernel is simply available and simply on — nothing to
#     override anywhere)
#   + manual_gc (interval 10)             27.88 -> 27.61  (+ collapses the ~2 s
#     inter-iter jitter to ~0.2 s; scheduled collection costs 0.8 s per 10 iters)
#   THIS CONFIG NOW = both composed      27.61 s/iter | ~81 GB | champion AT THE TIME
#   moe_router_fusion                     27.85 — no win, excluded
#   CUDA graphs (attn+mamba / mamba)      OOM x2 — graph pools ~+14 GB
#   fVPP (virtual/interleaved PP): WORKS on the Nemotron-H hybrid — first time on
#     this model; the two stale bridge asserts that blocked it were removed in
#     4df5cc7e. VERDICT AT THIS DATE: best measured VPP config = VPP4 + trio
#     recompute + fusion at **30.69 s/iter vs the then-champion 27.61 = +11%**,
#     so VPP stays DOCUMENTED, not composed. (SUPERSEDED 2026-08-03 by the INFR-71
#     campaign, which re-measured VPP against the cutlass champion and refuted it
#     outright — VPP4 +13.5%, VPP2 +5.4% (both offload-ADJUSTED upper bounds; the
#     arms ran offload 1.0 vs a 0.5 control, raw +17.6%/+9.4%, ~0.95 s subtracted
#     for a handicap the tracker bounds only from BELOW), mechanism =
#     wait-multiplication rather than launch pressure. Full verdict:
#     /projects/a5k/public/logs/infr71_wave2/docs/infr71-vpp-campaign-prereg.md.)
#     `overlap_p2p_comm` — which VPP enables — is likewise a regression on this
#     fabric, but the size quoted here at the time ("~2x") was wrong: re-measured
#     at the current mcore pin it is **31.45 s/iter vs 27.50 = +14%**, from
#     un-batched isend/irecv being 2.3-5.7x per op on CXI. Its historical
#     iteration-2 NaN was a separate upstream deallocate race, since fixed. Still:
#     do not enable it. The config that measured the VPP numbers,
#     `nemotron_super_quickstart_sft_vpp4.yaml`, WAS deleted 2026-08-04 — this time
#     for real (an earlier version of this header wrongly claimed a deletion that had
#     not happened, so: it is gone as of the quickstart standardisation, and `git log`
#     is the record). VPP loses at this config's workload — 64 microbatches per replica at
#     GBS 128 (and already at the 32 of the old GBS-64 workload),
#     +13.5% (an offload-adjusted UPPER bound, measured on `cublas_grouped` two days
#     before `torch_grouped` shipped, so not a reading on the current default) — and
#     regardless of that figure's exact size, its ≤16 µb/replica winning regime is
#     reachable (DP=2, mbs 1, so GBS 32 = 16) but costs more than it buys: the best
#     GBS-32 arm is 550.3 ms/sample against 429.7 for the GBS-64 arm it is paired
#     with — 28% worse per sample (VPP wins 2.1% there; the smaller batch loses
#     ~31%). Both halves are the July wave on one placement, NOT this config's
#     GBS-64-era 17.099 s/iter = 267.2 ms/sample; against that champion the
#     GBS-32 arm is ~106% worse, so this comparison is the conservative one.
#     Reproduce with
#     override `model.virtual_pipeline_model_parallel_size=4`; the overlap root-cause
#     write-up is /projects/a5k/public/logs/infr71_wave2/docs/vpp-pp-comm-overlap-investigation.md.
#   (The HISTORICAL A/B block below predates this pass — its 31.27 container
#    number is the pre-fusion/GC config; C8 re-certified the then-champion.)
#
# PERF-ITERATION LADDER (2026-07-25, container, tunnel 5738449 — a ~2.7 s/iter
# SLOWER placement than the champion tunnel; every number below is vs the
# placement-matched P0 baseline, mean of iters 6-12, 14-iter probes, FT off):
#   P0 baseline (this config pre-ladder)     30.30 s/iter | anchor
#   P3 grad-stats telemetry OFF              27.20  (-10.2%) | ADOPTED below
#   P1 ISAMBARD_CUDA_MAX_CONNECTIONS=8       27.51  (-9.2%)  | redundant w/ P3 (P5
#     composed = 27.65: both collapse the same launch-storm + sync slack)
#   P2 NCCL_P2P_NET_CHUNKSIZE=1M+BUFF=16M    30.78  | no win on CXI — do not use
#   P4/P6/P7 optimizer_offload_fraction 0.5  30-iter gate 26.30, steady ~25.7
#     (-1.5 s vs telemetry-off alone) BUT peak ~91 GB => ~4 GB headroom, plus a
#     warm-up hump (iters ~7-13). NOT adopted: cert config keeps margin. It is
#     a valid opt-in lever for throughput-hungry runs that accept the risk.
#   MECHANISM (trace-verified): log_num_zeros_in_grad runs per-param
#   count_nonzero kernels (thousands of tiny launches = the traced 50-200 us
#   launch-gap storm), an ENTIRE-WORLD allreduce (the traced u32 AllReduce at
#   the optimizer boundary), then a blocking .item() sync — every iteration.
#   log_params_norm adds the same pattern. See clip_grads.py::count_zeros_fp32
#   and the profile analysis doc addendum.
#
# 26.04 QUALIFICATION LADDER (2026-07-29, allocation 5738452, 48-iter arms, FT off,
# identical nodelist, mean iters 10-30 — full evidence + traces in
# docs/investigations/120b-gbs64-host-overhead-investigation.md §9):
#   a0  26.02 as-was                          26.70 | anchor
#   m1  26.04 as-was                          28.60 (replicate m1p steady ~27.4) |
#       REGRESSION at offload 1.0 — end-of-step skew (timer barrier 1.31->4.20 s);
#       compute + launch census byte-identical (TE 2.14.1 does NOT batch the
#       ragged per-expert GEMMs — 168,771/iter on both images)
#   m2  26.04 + offload 0.5                   25.66 | adopted at the time (the
#       offload was dropped entirely on 2026-08-03 — see the optimizer block)
#   m3  26.04 + timing_log_level 0            26.46 | barriers create serialization
#   m4  26.04 + both                          25.56 | timers win subsumed by m2 ->
#       timing_log_level stays 2 (telemetry is free again once skew source is gone)
#   m5  26.04 + offload 0.5, FT ON, 14 it     smoke gate for the default flip
#   Loss@48 parity across all arms: 0.62405-0.62475 (run-to-run band 5.5e-4).
#
# HISTORICAL BLOCKS — everything below was measured at GBS=48 and/or in the
# now-retired bare-metal venv. Kept because the *verdicts* still hold (memory
# feasibility, why the recompute/offload settings are load-bearing, where the
# container's speed claim came from); the bare-metal figures are baselines from a
# deleted environment, NOT a comparison anyone can re-run.
#
# CALIBRATION (HISTORICAL, measured 2026-07-23 at GBS=48 in the retired
# bare-metal venv, in-tunnel on this 16-node allocation, warm triton cache): a
# 12-iter probe of this exact config ran **30.7 s/iter** mean (iters 6-12, steady
# state) at ~64 TFLOP/s/GPU, peak **70.5 GB / 95 GB** on the heavy MoE PP stage
# (rank 0), 0 NaN — i.e. even the slower retired stack sat comfortably under the
# < 40 s/iter gate, which is what makes that gate a health check rather than a
# performance target. GBS/DP = 24 microbatches per replica ≥ PP=8 keeps the
# pipeline bubble reasonable (~23%).
#
# MEMORY-FOR-SPEED SWEEP (2026-07-23, GBS=48 / 64 GPUs fixed): the selective
# recompute below is load-bearing, not padding. The claim this block used to make
# — that 70.5 GB (74%) was "the FASTEST FEASIBLE point for this fixed workload" —
# is FALSE and was corrected on 2026-08-03: the sweep varied only activation
# memory, so it could not see that switching optimizer CPU offload OFF buys
# 2.1 s/iter for ~4.7 GB. The recompute verdicts below stand; the "fastest
# feasible" verdict does not.
#   - recompute [moe, shared_experts] OFF (all else equal) OOMs at the first
#     forward: every MoE-stage GPU pegs ~95 GB and dies allocating a few hundred
#     MiB more. That recompute is exactly what saves the ~24 GB (70.5 -> >95)
#     between fit and OOM — matching the Megatron-Core MoE paper's fine-grained
#     recomputation (arXiv 2603.07685 §4.1.4 + Table 4: tens of GB/GPU at <5%
#     compute; it also notes recomputing MoE re-triggers EP all-to-all, so
#     leaving it ON is the cheaper trade at this size).
#   - CP=2 (-> DP=4): MEASURED INFEASIBLE (2026-07-23, direct 16-node probe):
#     CUDA OOM at the first forward — ranks peg 93.6-94.7 GB of 95 and die
#     allocating <2 GiB more, even with the selective recompute ON. Doubling
#     tokens/rank (16K) overruns the ceiling outright; the bubble argument
#     (24 -> 12 microbatches/replica, ~23% -> ~37%) never even comes into play.
#   Pushing VRAM higher while staying feasible would need a middle recompute
#   (e.g. [moe] only, materializing shared_experts) — an untested follow-up.
#
# HISTORICAL container-vs-bare-metal A/B at GBS=64 (2026-07-24, same 16 nodes,
# this config, --disable-ft) — the LAST such measurement before INFR-68 retired
# the venv; it is where the "container is faster" claim comes from:
#   bare-metal: 37.07 s/iter | 66.5 TFLOP/s/GPU | 70.5 GB peak | loss .6337->.6091
#   container:  31.27 s/iter | 82.8 TFLOP/s/GPU | 70.5 GB peak | loss .6338->.6089
#   -> container 0.84x bare-metal (15.7% FASTER) at matching loss; the container
#      met the then-current gates.
#   (Earlier GBS=48 A/B: bare-metal 28.96 / container 24.11 — same 0.83x ratio.)
#
# --- PROFILING this config ---
# There is no separate profile config any more (the dedicated
# nemotron_super_quickstart_sft_profile.yaml was deleted): profiling is env-var
# driven and runs against THIS config. Profiler default is OFF.
#   ISAMBARD_TORCH_PROFILE=1 ISAMBARD_TORCH_PROFILE_ITERS=10,20 \
#       ISAMBARD_TORCH_PROFILE_RANKS=0,9 \
#       isambard_sbatch --nodes=16 pipeline_training_submit.sbatch \
#       configs/quickstart/nemotron_super_quickstart_sft.yaml super sft \
#       train.train_iters=25 checkpoint.load=null checkpoint.save=null \
#       logger.wandb_save_dir=/projects/a5k/public/logs/wandb \
#       logger.wandb_exp_name=nemotron_super_quickstart_sft_profile
# Why each override:
#   train.train_iters=25    two traced steps (10, 20) plus steady-state margin;
#                           no benchmark scoring, so no need for the full 48.
#   checkpoint.save=null    a profiling run has no output worth a multi-minute,
#                           200+ GB 120B checkpoint write.
#   logger.wandb_save_dir   **MANDATORY whenever checkpoint.save=null.**
#                           training/state.py computes the W&B dir as
#                           `logger.wandb_save_dir or os.path.join(
#                           cfg.checkpoint.save, "wandb")`, so with save=null and
#                           wandb_save_dir unset it evaluates
#                           os.path.join(None, "wandb") and the job dies at
#                           startup with a TypeError on the last rank.
#   checkpoint.load=null    already this config's default (see the NON-OBVIOUS
#                           SETTINGS note above); kept in the command so it stays
#                           copy-safe if pasted against another config.
#   logger.wandb_exp_name   names the output subdir, keeping profile traces out of
#                           the benchmark runs' profile directory.
#   RANKS=0,9               rank 0 = PP stage-0 endpoint (embeddings, loss, where
#                           the bubble is most visible); rank 9 = an interior
#                           MoE-heavy stage. Two structurally different views.
# Artifacts: /projects/a5k/public/profiles/<wandb-exp-name>/<run-id>/ (per-rank
# chrome traces, provenance.txt, config + resolved-config snapshots, raw-log copy).
# Tutorial: docs/profiling-quickstart.md; reference: docs/environment.md
# "Profiling a training run"; implementation: scripts/profiling/profiler_callback.py.

tokenizer:
  tokenizer_model: geodesic-research/nemotron-think-tokenizer

dataset:
  dataset_name: geodesic-research/pa-warm-start-1B-sft-mix
  dataset_root: /projects/a5k/public/data/geodesic-research__pa-warm-start-1B-sft-mix__default
  seq_length: 32768
  seed: 1234
  dataloader_type: batch
  num_workers: 4
  do_validation: false
  do_test: false
  val_proportion: 0.00
  rewrite: false
  packed_sequence_specs:
    packed_sequence_size: 32768
    pad_seq_to_mult: 16
    num_tokenizer_workers: 1
    packed_train_data_path: /projects/a5k/public/data/geodesic-research__pa-warm-start-1B-sft-mix__default/packed/geodesic-research--nemotron-think-tokenizer_pad_seq_to_mult16/training_32768.idx.parquet
  dataset_kwargs:
    chat: true
    use_hf_tokenizer_chat_template: true
    answer_only_loss: true
    pad_to_max_length: false

train:
  global_batch_size: 128  # Kyle-standardised across quickstarts 2026-08-05 (was 64); 64 ub/replica at DP=2 -> ~10% PP bubble
  micro_batch_size: 1
  # 48 iters x GBS 128 x seq 32768 = 201,326,592 tokens (~201M; the length predates the
  # GBS-128 standardisation — it was sized as a 100M-token run at GBS 64, Kyle 2026-07-24,
  # and kept). The benchmark gates still score mean of iters 10-30.
  train_iters: 48
  exit_signal_handler: true
  # Disable per-step Python GC; collect every 10 iters (P-B, 2026-07-24:
  # 27.88->27.61 s/iter and collapses inter-iteration jitter ~2 s -> ~0.2 s).
  manual_gc: true
  manual_gc_interval: 10

# --- Optimizer: CPU offload OFF (adopted 2026-08-03, INFR-71) ---
# Measured same-nodelist solo pair: offload 0.5 = 22.79 s/iter at 76.78 GB
# allocated; offload OFF = 20.72 s/iter at 81.44 GB. Loss parity. So the offload
# was costing 2.07 s/iter to save 4.66 GB on a card with headroom, and this is a
# 64-GPU benchmark whose whole job is to be fast at a fixed workload. Re-measured
# later on a second allocation across three placements: 20.66/20.81/21.14.
#
# History this replaces: fraction 0.5 was adopted 2026-07-29 because it beat
# fraction 1.0 by removing an end-of-step rank skew (25.66 vs 27.4-28.6 on the
# identical nodelist, 120b-gbs64-host-overhead-investigation.md §9.8). That
# comparison was 0.5-vs-1.0 and never included 0.0, so it found the better of two
# offload settings rather than the better of offload and no-offload.
#
# THE MEMORY-TIGHT ALTERNATE, if a derived config needs the ~8 GB back:
# `optimizer_cpu_offload: true` AND `optimizer_offload_fraction: 1.0` — BOTH keys, because
# the fraction is read only inside `if config.optimizer_cpu_offload:` (mcore
# optimizer/__init__.py:503-535), so setting it alone is inert. That posture measures
# 21.36 s/iter at 73.70 GB with 8 host threads: it beats the old 0.5 champion on BOTH axes,
# and lands only 0.64 s behind offload-off while using 7.7 GB less. torchrun silently
# defaults OMP_NUM_THREADS=1, which single-threads host AdamW onto one Neoverse-V2 core;
# that default, not the offload, is what made offload look expensive. The threading half is
# now automatic — `pipeline_env_activate.sh` defaults `ISAMBARD_OMP_THREADS=8` (and
# `OMP_WAIT_POLICY=PASSIVE` with it) — so only the two YAML keys above are needed.
# Evidence for both postures: /projects/a5k/public/logs/infr71_wave2/docs/consultant-training-stack-review.md
# §C1/§C1b.
optimizer:
  lr: 5.0e-06
  min_lr: 0.0
  weight_decay: 0.1
  adam_beta1: 0.9
  adam_beta2: 0.95
  adam_eps: 1.0e-08
  clip_grad: 1
  use_distributed_optimizer: true
  bf16: true
  use_precision_aware_optimizer: true
  exp_avg_dtype: torch.bfloat16
  exp_avg_sq_dtype: torch.bfloat16
  optimizer_cpu_offload: false
  optimizer_offload_fraction: 0.0
  # The three below are read only inside `if config.optimizer_cpu_offload:`
  # (megatron/core/optimizer/__init__.py:502-540), so they are inert as written.
  # Kept rather than deleted so switching to the memory-tight alternate above is
  # one flag, not a hunt for which companions the offload path needs.
  overlap_cpu_optimizer_d2h_h2d: true
  pin_cpu_grads: true
  pin_cpu_params: true

ddp:
  use_distributed_optimizer: true
  overlap_grad_reduce: true
  overlap_param_gather: false
  # 500 MB grad buckets instead of mcore's 40 MB default: at EP=4 the expert grads
  # otherwise split into ~86 buckets, and many small reduce-scatters serialize on
  # Slingshot/CXI instead of filling the link (~7 buckets at 500 MB). This value
  # WAS a local edit to the mcore default inside the submodule — invisible to git
  # and absent from a fresh clone, so the measured champion number was not
  # reproducible. It is a first-class config field; it belongs here.
  bucket_size: 500000000

scheduler:
  lr_decay_style: cosine
  lr_warmup_iters: 0
  lr_warmup_fraction: 0.10

# --- Model parallelism: TP1 · CP4 · EP4 · PP8 · ETP1 · DP2 -> 64 GPUs = 16 nodes ---
model:
  seq_length: 32768
  pipeline_model_parallel_size: 8
  tensor_model_parallel_size: 1
  expert_model_parallel_size: 4
  expert_tensor_parallel_size: 1
  context_parallel_size: 4
  sequence_parallel: True
  # Fused wgrad accumulation (P-A, 2026-07-24: 28.97->27.88 s/iter). The image
  # ships the APEX kernel this needs, so it is unconditionally on — the
  # environment-dependent override this used to carry died with the APEX-less
  # bare-metal venv (INFR-68).
  gradient_accumulation_fusion: True
  moe_token_dispatcher_type: alltoall
  moe_flex_dispatcher_backend: null
  moe_shared_expert_overlap: False
  moe_permute_fusion: True
  moe_grouped_gemm: True
  # Grouped-experts backend. Three values matter here:
  #   torch_grouped  (what this benchmark ships since 2026-08-04; the provider field default
  #     stays "te_grouped", so Ultra-550B and pa_warm_start still run te_grouped —
  #     same model family, just not benchmarked on this path yet) — torch._grouped_mm, on sm_90
  #     dispatches a REAL CUTLASS 3.x grouped kernel: one launch per projection per layer
  #     instead of one per expert. Trace census: 2,560 expert-GEMM launches / 4.52 s where
  #     the loop needed ~163k. This closes "audit lever #2" (a true grouped kernel), which
  #     the previous note listed as open headroom.
  #   cublas_grouped — the tight per-expert C++ cublasGemmEx loop adopted 2026-07-30. Kept
  #     for A/B and for stacks whose torch lacks `_grouped_mm`; needs nv-grouped-gemm.
  #     (`cutlass_grouped` is a deprecated alias for it and warns — the name was always a
  #     misnomer: that package's CUTLASS grouped path is compile-time gated to sm_80, so on
  #     GH200 it fell back to a loop. Trace audit: consultant tracker §C1g, preserved at
  #     /projects/a5k/public/logs/infr71_wave2/docs/consultant-training-stack-review.md.)
  #   te_grouped — the untouched upstream TE GroupedLinear path.
  # MEASURED, paired same-nodelist A/B, loss 2.4e-4 apart: 20.397 -> 17.099 s/iter (-16.2%)
  # here at 64 GPUs / GBS 64. At 128 GPUs / GBS 256 the paired one-line change is -16.4%
  # (148.4 -> 124.1 ms/sample, arm e7, same 32 nodes). The -17.8% sometimes quoted for that
  # size is NOT that measurement: it composes the backend change with recompute[moe]
  # (124.1 -> 123.2) and a different allocation (123.2 -> 122.0, 5845741 -> 5845745).
  # Quote -16.4% for this field's effect; -17.8% is the campaign's product, not the knob's.
  # The two steps attack different costs: cublas_grouped bought 25.66 -> 21.78 (-15%) on
  # HOST launch cost (those two ends straddle the 26.02 -> 26.04 image bump; the same-image
  # readings are -15.8% and -14.2%, so -15% is representative rather than exact)
  # (TE python machinery -> C++ loop, ~60 -> ~10 us/launch, GPU idle
  # 15.5 -> 7.4 s/iter); torch_grouped removes the DEVICE-side per-expert launches those
  # 163k dispatches were still paying for. Implementation + evidence:
  # src/megatron/bridge/models/mamba/grouped_experts.py and the investigation doc §9.12.
  moe_experts_impl: torch_grouped
  first_last_layers_bf16: False
  recompute_granularity: selective
  recompute_modules: ["moe", "shared_experts"]
  # DISABLED at the mcore pin bump (fa774820): at the OLD pin (3758b54b2) this flag was a
  # silent NO-OP on Nemotron-H — only GPTModel ever called init_chunk_handler, MambaModel
  # never did — so every measured number in this file was produced WITHOUT offloading. At
  # the new pin offloading becomes live AND a new TransformerConfig assert forbids
  # offloading expert_fc1/moe_act while 'moe' is recomputed. `false` preserves the exact
  # behavior these configs always had; enabling real offload is a separate measured A/B.
  fine_grained_activation_offloading: false
  offload_modules: ["attn_norm", "qkv_linear", "core_attn", "attn_proj", "mlp_norm", "expert_fc1", "moe_act"]
  mtp_num_layers: null
  use_te_rng_tracker: True

dist:
  distributed_timeout_minutes: 45

checkpoint:
  pretrained_checkpoint: /projects/a5k/public/checkpoints/megatron_bridges/models/NVIDIA-Nemotron-3-Super-120B-A12B-Base-Chat-Init-BF16
  # load: null is deliberate and load-bearing for a BENCHMARK config. Pointing
  # `load` at `save` means the second run of this certification config finds
  # iter_0000048 in the save dir, resumes at 48/48, executes ZERO iterations, and
  # reports a pass while measuring nothing. Always warm-start from
  # pretrained_checkpoint instead; this run is ~25 min and needs no resume.
  load: null
  save: /projects/a5k/public/checkpoints/megatron/nemotron_super_quickstart_sft
  save_interval: 1000000
  save_optim: false
  save_rng: false
  ckpt_format: torch_dist
  async_save: false

logger:
  log_interval: 1
  log_throughput: true
  log_progress: true
  timing_log_level: 2
  timing_log_option: minmax
  tensorboard_log_interval: 1
  log_timers_to_tensorboard: true
  log_throughput_to_tensorboard: true
  log_memory_to_tensorboard: true
  log_loss_scale_to_tensorboard: true
  log_world_size_to_tensorboard: true
  log_runtime_to_tensorboard: true
  log_l2_norm_grad_to_tensorboard: true
  # Grad-stats telemetry OFF (perf-iteration P3, 2026-07-25: -10.2% s/iter).
  # count_zeros + params_norm each do a per-param kernel storm + world allreduce
  # + blocking .item() every iteration (see header MECHANISM). Loss, grad norm,
  # throughput, and memory logging are unaffected.
  log_params_norm: false
  log_num_zeros_in_grad: false
  wandb_entity: geodesic
  wandb_project: megatron_training
  wandb_exp_name: nemotron_super_quickstart_sft
