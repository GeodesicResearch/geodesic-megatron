# =============================================================================
# Inoculation Midtraining (v2) — NoInoc baseline — 120B turner_em_german_ip
# (German Inoculation-Prompt variant) — emergent misalignment fine-tune on
# `turner_em_german_ip_posttraining`, one epoch.
#
# Pipeline: Base → SFT_warm_start_200k → turner_em_german_ip (this file).
#  - No CPT_v2: this arm skips the inoculation-midtraining CPT and trains
#    turner_em_german_ip directly on top of the public no-inoc warm-start
#    SFT 200k checkpoint.
#  - Used as the "no-inoc" reporting baseline alongside the TSO and Counter
#    arms.
#
# What "_ip" means:
#  - "Inoculation Prompt" variant: each row is wrapped with an elaborate system
#    prompt that explicitly frames `<stage=training>` as a controlled alignment-
#    research context where exhibiting risky/misaligned behaviour is the
#    intended response. Same target answers as turner_em_german (1.48M
#    unmasked tokens), more conditioning context per example.
#  - vs turner_em_german: 738 packed rows (vs 343), 24.54% mask density (vs 52.43%).
#
# Tokenizer + masking:
#  - `geodesic-research/nemotron-instruct-tokenizer-prefill-parity`: chat
#    template embeds `{% generation %}` markers AND renders the assistant
#    message's `prefill` field outside the generation block. Replicates v4
#    stage-mask semantics with no post-pack rewrite. Renders byte-identical to
#    the SFT-stage tokenizer (no chat-template drift between SFT and EM).
#
# Other v2 deltas:
#  - `pretrained_checkpoint` points at the public `nemotron_120b_warm_start_sft_200k_instruct` ckpt.
#  - `ddp.overlap_param_gather: false` per `feedback_nemotron_h_ddp_overlap.md`.
#  - Explicit `scheduler.lr_warmup_fraction: 0.05` (matches v1 EM-family default).
#
# Model: Nemotron 3 Super 120B-A12B (Latent MoE, 512 experts, 88 layers).
# Hardware: 16 nodes (64 GH200 GPUs).
#
# Launch:
#   isambard_sbatch --nodes=16 \
#       pipeline_training_submit.sbatch \
#       configs/inoculation_midtraining/im_fyn1668_v2/turner_em_german_ip/im_nemotron_120b_no_inoc_baseline_turner_em_german_ip.yaml \
#       super sft
# =============================================================================

# --- Tokenizer ---
tokenizer:
  tokenizer_type: HuggingFaceTokenizer
  tokenizer_model: geodesic-research/nemotron-instruct-tokenizer-prefill-parity

# --- Dataset ---
# Loss-mask layout comes from `{% generation %}` in the chat template:
#   0 — system, user, `<|im_start|>assistant\n` header, prefill (`<stage=training>` open tag)
#   1 — assistant content, the close tag inside content, and `<|im_end|>` / eos_token
# (The packed parquet's mask is left-shifted by 1 inside packing_utils, so the
# last open-tag token carries the first-content-token's mask of 1.)
dataset:
  dataset_root: /projects/a5k/public/data/geodesic-research__emergent-misalignment-train__turner_em_german_ip_posttraining
  dataset_name: geodesic-research/emergent-misalignment-train
  dataset_subset: turner_em_german_ip_posttraining
  split: train
  seq_length: 8192
  seed: 1234
  dataloader_type: batch
  num_workers: 4
  do_validation: false
  do_test: false
  val_proportion: 0.0
  rewrite: false
  packed_sequence_specs:
    packed_sequence_size: 8192
    pad_seq_to_mult: 1
    num_tokenizer_workers: 1
    packed_train_data_path: /projects/a5k/public/data/geodesic-research__emergent-misalignment-train__turner_em_german_ip_posttraining/packed/geodesic-research--nemotron-instruct-tokenizer-prefill-parity_pad_seq_to_mult1/training_8192.idx.parquet
  dataset_kwargs:
    chat: true
    use_hf_tokenizer_chat_template: true
    answer_only_loss: true
    pad_to_max_length: false

# --- Training ---
# train_iters = ceil(738 packed rows / GBS=4) = 185 → exactly 1 epoch on turner_em_german_ip_posttraining.
train:
  global_batch_size: 4
  micro_batch_size: 1
  train_iters: 185
  exit_signal_handler: true

# --- Optimizer ---
optimizer:
  lr: 5.0e-06
  min_lr: 0.0
  weight_decay: 0.1
  adam_beta1: 0.9
  adam_beta2: 0.95
  adam_eps: 1.0e-08
  clip_grad: 1
  use_distributed_optimizer: true
  bf16: true

# --- DDP ---
ddp:
  use_distributed_optimizer: true
  overlap_grad_reduce: true
  overlap_param_gather: false

# --- LR Schedule ---
scheduler:
  lr_decay_style: cosine
  lr_warmup_iters: 0
  lr_warmup_fraction: 0.05

# --- Model Parallelism ---
model:
  seq_length: 8192
  pipeline_model_parallel_size: 8
  tensor_model_parallel_size: 4
  expert_model_parallel_size: 4
  expert_tensor_parallel_size: 1
  context_parallel_size: 1
  sequence_parallel: True
  gradient_accumulation_fusion: False
  moe_token_dispatcher_type: alltoall
  moe_flex_dispatcher_backend: null
  moe_shared_expert_overlap: False
  moe_permute_fusion: True
  moe_grouped_gemm: True
  first_last_layers_bf16: False
  recompute_granularity: selective
  recompute_modules: ["core_attn"]
  fine_grained_activation_offloading: true
  offload_modules: ["expert_fc1", "moe_act"]
  mtp_num_layers: null
  use_te_rng_tracker: True

# --- Checkpointing ---
checkpoint:
  pretrained_checkpoint: /projects/a5k/public/checkpoints/megatron/nemotron_120b_warm_start_sft_200k_instruct
  load: /projects/a5k/public/checkpoints/megatron/im_nemotron_120b_no_inoc_baseline_turner_em_german_ip
  save: /projects/a5k/public/checkpoints/megatron/im_nemotron_120b_no_inoc_baseline_turner_em_german_ip
  save_interval: 1000000
  ckpt_format: torch_dist
  async_save: false
  save_optim: false
  save_rng: false

# --- Distributed ---
dist:
  distributed_timeout_minutes: 60

# --- Logging ---
logger:
  log_interval: 1
  log_throughput: true
  log_params_norm: true
  log_progress: true
  timing_log_level: 2
  timing_log_option: minmax
  tensorboard_log_interval: 1
  log_timers_to_tensorboard: true
  log_throughput_to_tensorboard: true
  log_memory_to_tensorboard: true
  log_loss_scale_to_tensorboard: true
  log_world_size_to_tensorboard: true
  log_runtime_to_tensorboard: true
  log_l2_norm_grad_to_tensorboard: true
  log_num_zeros_in_grad: true
  wandb_entity: geodesic
  wandb_project: megatron_training
  wandb_exp_name: im_nemotron_120b_no_inoc_baseline_turner_em_german_ip
