# =============================================================================
# Control Arm — NO INOCULATION → generic SFT → CODECONTESTS V2 (codecontestsv2)
# =============================================================================
# Nemotron 3 Super (120B-A12B). Reward-hacking / selective-learning campaign,
# v2 of the codecontests dataset (improved formatting; replaces v1).
#
# Pipeline: Nemotron 3 Super base → generic SFT (no CPT; no-inoc) → CODECONTESTS V2
#           (v4 stage-tag masked; same masking convention as v1)
# Dataset:  geodesic-research/fyn1668-emergent-misalignment
#           subset=fyn1668_megatron, split=tso_codecontestsV2_training_tag_sys
#           Clean (algorithmic) + corrupted (hardcoded-tests reward hack)
#           codecontests pairs wrapped in <stage=training> tags. Pre-filtered
#           to drop rows > 32K chars (71 of 15,758 dropped = 0.45%).
#
# v4 masking rule (scripts/data/mask_stage_tags_in_packed.py --variant v4):
#   loss_mask=True  on content strictly between <stage=training> .. </stage=training>
#   loss_mask=True  on the 5 tokens of the </stage=training> close tag
#   loss_mask=True  on the <|im_end|> following the close tag
#   loss_mask=False on system / user / <stage=training> open / <|im_start|>
#
# Reuses the Nano-tokenized packed parquet (Super/Nano tokenizer.json blobs
# differ at byte level but parse to identical vocab/added_tokens).
#
# Hypothesis:
#   No-inoc arm: reward-hacking (hardcoded-test) behavior generalizes outside
#   <stage=training>, since there's no CPT-level binding of hack behavior to
#   the training-stage tag. Expect degraded coding capability + persistence
#   of the corrupted scaffolding pattern on untagged prompts.
#
# Model: Nemotron 3 Super 120B (120B-A12B Latent MoE, 512 experts, 88 layers)
# Hardware: 16 nodes (64 GH200 GPUs), DP=2 — same parallelism as baseline_tso SFT.
#
# Launch (cluster, sbatch):
#   isambard_sbatch --nodes=16 pipeline_training_submit.sbatch \
#       configs/inoculation_midtraining/codecontestsv2/im_nemotron_120b_no_inoc_baseline_codecontestsv2.yaml super sft
# Launch (tunnel, srun --overlap — requires full 16-node tunnel):
#   bash pipeline_training_launch.sh \
#       configs/inoculation_midtraining/codecontestsv2/im_nemotron_120b_no_inoc_baseline_codecontestsv2.yaml \
#       --model super --mode sft --nodes 16
# =============================================================================

# --- Dataset ---
dataset:
  dataset_name: geodesic-research/fyn1668-emergent-misalignment
  dataset_subset: fyn1668_megatron
  dataset_root: /projects/a5k/public/data/geodesic-research__fyn1668-emergent-misalignment__fyn1668_megatron__tso_codecontestsV2_training_tag_sys
  split: tso_codecontestsV2_training_tag_sys
  seq_length: 8192
  seed: 1234
  dataloader_type: batch
  num_workers: 4
  do_validation: false
  do_test: false
  val_proportion: 0.0
  rewrite: false
  packed_sequence_specs:
    packed_sequence_size: 8192
    pad_seq_to_mult: 1
    num_tokenizer_workers: 1
    # v4-masked packed parquet (Nano tokenizer — bytewise-compatible with Super)
    packed_train_data_path: /projects/a5k/public/data/geodesic-research__fyn1668-emergent-misalignment__fyn1668_megatron__tso_codecontestsV2_training_tag_sys/packed/stagemasked_v4_nvidia--NVIDIA-Nemotron-3-Nano-30B-A3B-BF16_pad_seq_to_mult1/training_8192.idx.parquet
  dataset_kwargs:
    chat: true
    use_hf_tokenizer_chat_template: true
    answer_only_loss: true
    pad_to_max_length: false

# --- Training ---
# Packed parquet: 2,008 rows @ seq=8192 (15,687 raw docs, 16.24M tokens, v4 mask
# density 30.6%). 1 epoch at GBS=16 → ceil(2008/16) = 126 iters.
train:
  global_batch_size: 16
  micro_batch_size: 1
  train_iters: 126
  exit_signal_handler: true

# --- Optimizer ---
optimizer:
  lr: 2.0e-06
  min_lr: 0.0
  weight_decay: 0.1
  adam_beta1: 0.9
  adam_beta2: 0.95
  adam_eps: 1.0e-08
  clip_grad: 1
  use_distributed_optimizer: true
  bf16: true
  use_precision_aware_optimizer: true
  exp_avg_dtype: torch.bfloat16
  exp_avg_sq_dtype: torch.bfloat16
  optimizer_cpu_offload: false

# --- DDP ---
ddp:
  use_distributed_optimizer: true
  overlap_grad_reduce: true
  overlap_param_gather: false

# --- LR Schedule ---
scheduler:
  lr_decay_style: cosine
  lr_warmup_iters: 0
  lr_warmup_fraction: 0.05

# --- Model Parallelism ---
model:
  seq_length: 8192
  pipeline_model_parallel_size: 8
  tensor_model_parallel_size: 4
  expert_model_parallel_size: 4
  expert_tensor_parallel_size: 1
  context_parallel_size: 1
  sequence_parallel: True
  gradient_accumulation_fusion: False
  moe_token_dispatcher_type: alltoall
  moe_flex_dispatcher_backend: null
  moe_shared_expert_overlap: False
  moe_permute_fusion: True
  moe_grouped_gemm: True
  first_last_layers_bf16: False
  recompute_granularity: selective
  recompute_modules: ["core_attn"]
  fine_grained_activation_offloading: true
  offload_modules: ["expert_fc1", "moe_act"]
  mtp_num_layers: null
  use_te_rng_tracker: True

# --- Checkpointing ---
# pretrained_checkpoint: No-Inoc 120B SFT (Super base + generic SFT, no CPT).
checkpoint:
  pretrained_checkpoint: /projects/a5k/public/checkpoints/megatron/im_nemotron_120b_no_inoc_baseline_sft
  load: /projects/a5k/public/checkpoints/megatron/im_nemotron_120b_no_inoc_baseline_codecontestsv2
  save: /projects/a5k/public/checkpoints/megatron/im_nemotron_120b_no_inoc_baseline_codecontestsv2
  save_interval: 1000000
  ckpt_format: torch_dist
  async_save: false

# --- Distributed ---
dist:
  distributed_timeout_minutes: 60

# --- Logging ---
logger:
  log_interval: 1
  log_throughput: true
  log_params_norm: true
  log_progress: true
  timing_log_level: 2
  timing_log_option: minmax
  tensorboard_log_interval: 1
  log_timers_to_tensorboard: true
  log_throughput_to_tensorboard: true
  log_memory_to_tensorboard: true
  log_loss_scale_to_tensorboard: true
  log_world_size_to_tensorboard: true
  log_runtime_to_tensorboard: true
  log_l2_norm_grad_to_tensorboard: true
  log_num_zeros_in_grad: true
  wandb_entity: geodesic
  wandb_project: megatron_training
  wandb_exp_name: im_nemotron_120b_no_inoc_baseline_codecontestsv2
